# Negative control for gitleaks (tests/unit/test_gitleaks_fixtures.py).
#
# Same AWS-documented example credential as
# positive_control_credential.txt in this directory, which gitleaks'
# aws-access-token rule matches on its own. This file's path is explicitly
# listed in .gitleaks.toml's allowlist, so a scan that includes this file
# must still exit clean -- proving the allowlist mechanism used for
# .env.example and the BYOK test fixtures actually suppresses a
# known-safe, approved placeholder rather than merely happening to miss it.
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
