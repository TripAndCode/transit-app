[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transit-delay-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9",
    "pandas>=2.2",
    "ollama>=0.2",
    "sentence-transformers>=2.7",
    "asyncpg>=0.29",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
]

[tool.setuptools]
packages = ["pipeline", "pipeline.query", "api", "api.routers", "tests"]

[tool.setuptools.package-data]
pipeline = ["query/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
